# KP B6.3 — shellcheck & Common Pitfalls
**catching problems automatically with shellcheck: SC2086 · SC2046 · SC2006 · SC2164 · SC2115 · SC2181 · SC2004 · SC2148**

**8 tasks · ⭐ · RHEL 10 · server1 · for each: name the shellcheck warning and fix it**

> `shellcheck` is the static analyzer for bash — it flags the traps from B6.2 **automatically**. Install on RHEL with `sudo dnf install -y epel-release && sudo dnf install -y ShellCheck`, then run `shellcheck yourscript.sh`.
> Every task here is a classic shellcheck warning — name the problem first, then fix it.

---

**1.** shellcheck flags `SC2086` here — why, and the fix?
```bash
for f in $files; do cp $f /backup; done
```

**2.** `SC2046` fires on this — what breaks and how to fix?
```bash
rm $(find . -name '*.tmp')
```

**3.** `SC2006` — what's outdated here?
```bash
d=`date +%F`
```

**4.** `SC2164` — what's the risk?
```bash
cd /var/log
rm -f *.old
```

**5.** `SC2115` — why is this dangerous if `$TARGET` is empty?
```bash
rm -rf "$TARGET/"
```

**6.** `SC2181` — what's the anti-pattern?
```bash
grep -q root /etc/passwd
if [ $? -ne 0 ]; then echo "no root"; fi
```

**7.** `SC2004` — what's unnecessary?
```bash
n=$(( $x + 1 ))
```

**8.** `SC2148` — what's missing from the top of the script?
```bash
echo "starting backup"
```

---
*Ask "B6.3 answers" when done. That completes B6 🎉*
